### 10 年または 15 年前にさかのぼる履歴データで日常業務で使用されていないデータに最適なデータベース ワークロードはどれですか？

= データウェアハウス

### 正規化の主な利点は何ですか?

= データ重複の削減, 入力時の一般的なタイプミスやエラーを軽減

### 人、場所、モノの関係など、関係に大きく依存するデータには、どのようなデータベース スタイルが理想的ですか

= グラフ データ

### 「なぜそれが起こったのか」という疑問に答える分析にはどのような種類があるか (初回顧客の売上と長期顧客の売上の比較など)

= 診断分析

### ページ分割されたレポートの良い例は何ですか?

= 請求書

### ビジネスの日常業務では、どのタイプのデータベース・ワークロードが使用されていますか

= OLTP

### データウェアハウスの一般的な特性は、次のうちどれですか

= 大量のデータをサポート, 非命名化によって読み取り用に最適化されたデータベース

### SQL Server Analysis Services (SSAS)、Azure Analysis Services、および同様のアプリで使用されるディープ分析の種類は、どの種類のデータベース ワークロードですか

= OLAP

### SQL Database では、2 つの購入モデルがサポートされています。彼らは何ですか?

= DTU ベースと仮想コア

### Azure Data Studio でサポートされているオペレーティング システム プラットフォームは、次のどれですか

= Windows、Mac、Linux

### 次の Azure データベースのうち、どれが "サービスとしてのインフラストラクチャ" と見なされますか。いずれかを選択します

= VM内のSQL Server

### JSON データなどの非リレーショナル データを処理するために特別に設計された Azure Database 製品はどれですか

= CosmosDB

### 次にどの映画を観るかを推奨するなど、「何をすべきか」という質問に答える分析の種類はどれですか

= 規範的

### どのタイプの SQL コマンドが、表スキーマの作成や変更など、データベースの管理を処理しますか

= DDL

### 昨日の売上レポートなど、「何が起こったのか」という質問に答える分析の種類はどれですか?

= 記述分析 | 説明

### リレーショナル・データベースの共通特性は、次のうちどれですか

= テーブル、ビュー、プライマリキー、外部キー、インデックス、テーブル間のリレーションシップ、データベースによって強制される整合性

### 次の Azure データベース サービスのうち、独自の環境で実行されている SQL Server とほぼ 100% の互換性があるのはどれですか

= SQL Server マネージド インスタンス

### 次の Azure データベース サービスのうち、独自の環境で実行されている SQL Server との 100% の互換性が保証されているのはどれですか

= VM内のSQL Server

### Azure Synapse Analytics では、以下を使用したデータの並列処理がサポートされています

= 複数の計算ノードにまたがる処理を分割

### コースがテーブル名で、user1がデータベースユーザであると仮定すると、'GRANT SELECT ON コースTO USER1'は、次の例です

= データコントロール言語 (DCL): アクセス許可とその他のコントロールを管理します。

### 'CREATE DATABASE yourtestdb' は、次の例です

= データ定義言語 (DDL): データベース オブジェクトの構造を作成および変更します。

### 列ファミリ データベースはどれですか

= Azure Cosmos DB Cassandra API

### Azure Cosmos DB Gremlin API Graph データベースは Gremlin Language を使用して照会できます

= TRUE

### Azure ストレージ アカウントは、Azure Data Lake Storage に対してのみ、フォルダー レベルとファイル レベルでロール ベースのアクセス制御をサポートします

= TRUE

### Power BI では、ダッシュボードは ____ を使用して作成できます

= Power BI Service

### これらのデータ ストアのうち、データ間の複雑なリレーションシップを格納するために使用できるのはどれですか。(例:組織内の従業員間の仕事上の関係)

= CosmosDB Gremlin API

### クエリの結果セットに基づいてコンテンツを表す仮想テーブルはどれですか?(クエリ内の複数のテーブルを結合したり、計算列を持つことができます)

=ビュー: クエリにマップされた仮想テーブル。SQL クエリのテーブルと同じように使用できます。

### Azure SQL Database と Postgresql は、__ を使用するときに多要素認証メカニズムをサポートしています

= Active Directory

### ツリーマップは、四角形内にネストされた色付きの四角形を持つグラフです。色付きの四角形のサイズは値を表します

= TRUE

### Spark ジョブを実行できるのは、次のサービスのどれですか

= Azure Databricks, Azure HDInsight

### Azure Databricks はどのサービスからデータを消費できますか?

= Azure Blob storage、Azure Data Lake Store、Hadoop ストレージ、フラット ファイル、データベース、およびデータ ウェアハウスからのデータの処理

### これらの Azure サービスのどれがデータ ウェアハウジングとビッグ データ ワークロードに推奨されますか

= Azure Synapse Analytics

### オンプレミスとクラウドで実行されている仮想マシンが複数ある。これらすべてのマシンに接続されているネットワーク共有ドライブをマップする場合。これらの Azure サービスのどれをお勧めしますか?

= Azure ファイル

### サーバー メッセージ ブロック (SMB) プロトコルとネットワーク ファイル システム (NFS) プロトコルをサポートしているサービスはどれですか

= Azure ファイル

### データ分析カテゴリの決定:テキストの意味分析(レビューの分析)

= コグニティブ分析

### アクティビティの分類: 別のパイプライン アクティビティの出力に基づいてパイプライン アクティビティを調整する

= 制御フロー

### Data Factory から外部ソースへの接続 (外部接続の詳細の格納) に使用できるのは、これらのうちどれですか

= リンクされたサービス

### オンライン SaaS Power BI は何と呼ばれていますか?

= Power BI サービス

### これらのうち、"Power BI の 1 つ以上の視覚化のページ" を表すのはどれですか

= レポート

### これらのうち、どれが「1 つ以上のレポートからの単一ページの視覚化」を表しますか

= ダッシュボード

### データをキーと値のペアとして格納するすべての Azure 非リレーショナル データ ストアに名前を付けます

= Cosmos DB のテーブル ストレージ、テーブル ストレージ、および Redis キャッシュの機能です。

### 2つのリレーショナルデータテーブル間に外部キー関係を持つことの結果はどれですか?

= NULL 以外の値が FOREIGN KEY 制約の列に入力される場合、その値は参照先の列に存在していなければなりません。それ以外の場合は、外部キー違反エラー・メッセージが返されます。

### リレーショナル データ ストアでは、データ トランザクション ボリュームが高すぎてデータベースのパフォーマンスが低下した場合、これらのデータ トランザクションを複数のサーバーに分散する一般的でありながら困難な方法は、次のうちどれですか

= 手動シャーディング

### キーと値のストレージをサポートする非リレーショナル・データ・ストアが必要です。これは、別の長期的なデータ形式に移行する前に並べ替え、処理、クリーンアップする必要がある大量のデータが予想されるため、Azure にデータを格納するための最もコスト効率の高い方法である必要があります

= Azure テーブル ストレージ

### 次のデータ型のうち、非構造化データと呼ぶことができるのはどれですか

= Blobストレージのコンテナの内容

### Azure Blob Storage のどの機能が有効になっている場合、一定期間内に以前に削除されたファイルを取得できますか

= 論理的な削除 | Soft Delete

### どの Azure データ サービスが、あるソースから別の送信先にデータを移動するのに最適で、途中でデータを変換 (処理) する機能を備えていますか

= Azure Data Factory

### SQL Server のクラスタ化インデックスと非クラスタ化インデックスの違いは何ですか

= クラスター化インデックスは、キー値に基づいてテーブルまたはビュー内のデータ行を並べ替えて格納します。非クラスタ化インデックスは、データ行とは別の構造を持ちます。

### Azure テーブル ストレージとはどのような種類のストレージですか?

= 非リレーショナル構造化データ (構造化 NoSQL データとも呼ばれます) をクラウドに格納するサービスで、スキーマレス設計のキー/属性ストアを提供します。

### データセキュリティに関して言えば、最小特権の原則は何ですか?

= タスクを完了するために必要な最小限の特権を付与します。

### ACME 組織の開発者であり、顧客データベースに対して基本的な SQL SELECT クエリを実行します。この電話番号 - XXX-XXX-8866 など、一部のフィールドは部分的に非表示に戻されます。データを非表示にするためにどのような SQL データベース機能が使用されていますか

= 動的データ・マスキング (DDM)

### Azure Blob Storage にクール アクセス層を使用すると、何が得られますか

= ホット層よりもデータを格納する方が約 25% 安価ですが、データの読み取りと書き込みには 2 倍のコストがかかります。

### Azure SQL Database の読み取りと書き込みを 1 秒あたり約 100 回行う運用アプリケーションがあります。場合によっては、アプリケーション内で例外 "40501 - サービスは現在ビジー状態です" が発生します。このタイプのエラーの考えられる原因は何ですか?そして、そのための緩和策は何ですか?

= IOPS の制限は、データベース層の DTU の数に関連しています。より高いレベルに移行すると、これらの制限が増加します。

### 次のメトリックのうち、Azure Redis Cache インスタンスのコストに影響するのはどれですか

= 時間単位で課金され、リージョンによって異なります。また、選択したレベルによっても異なります。

### Azure の責任共有モデルにおける "データ セキュリティ" の責任は誰ですか

= お客様、およびデータベースプロバイダーの間で共有

### Azure Data Lake Storage Gen2 では、どのような種類のファイル システムが使用されていますか?

= Hadoop ファイル システム (HDFS)

### Azure ストレージ アカウントをデプロイするときに [ゾーン冗長ストレージ (ZRS)] を選択した場合、Azure はデータのコピーをいくつ保持しますか

= ゾーン冗長ストレージ (ZRS) は、プライマリ リージョンの 3 つの Azure 可用性ゾーン間でデータを同期的にコピーします。

### Azure ストレージ アカウントとの間で送受信されるすべてのデータをセキュリティで保護する方法の 1 つは何ですか

= ストレージ アカウントに [セキュリティで保護された転送が必要] プロパティを設定する

### 次のアクティビティのうち、Azure Data Factory の制御フローの一部と見なされるアクティビティはどれですか

= 条件、フィルター処理、およびパイプライン

### Azure テーブル ストレージにはどのような種類のデータが格納されますか?

= キーと値のペア

### データのセキュリティ保護に関して、「保存データ」を最もよく定義するのは、次の項目のどれですか

= 物理メディア (磁気ディスクか光ディスクか、オンプレミスまたはクラウド) に静的に存在するデータ。

### 既定では、Cosmos DB のセキュリティ モデルは何ですか

= 承認トークン

### ドキュメント (JSON) データで最適に動作する CosmosDB API 形式はどれですか

= コア (SQL) API 

### Cosmos DB では、主キー モデルを使用して、ユーザーが Cosmos DB アカウントを完全に制御できるようにします。データベースへの読み取り専用アクセスのみを持つように誰かを制限するにはどうすればよいですか?

= リソーストークン

### 次のデータベース モデルのうち、実行に数時間かかる、またはコンピューターが他のタスクの処理から解放される可能性がある、深く複雑なクエリ用に最適化されているのはどれですか

= OLAP 分析ワークロード

### 透過的なデータ暗号化 (TDE) はどのような種類のセキュリティを提供しますか

= SQL Server、Azure SQL Database、および Azure Synapse Analytics のデータ ファイルを暗号化します。この暗号化は、保存データの暗号化と呼ばれます。

### どの CosmosDB API 形式がキー値データで最適に機能しますか?

= テーブル API

### 次の文のうち、SQL Server のビューとは何かについて最もよく表しているのはどれですか

= ビューは、その内容がクエリによって定義される仮想テーブルです。

### Azure Table Storage の代わりに Azure Cosmos DB Table API を選ぶのはなぜでしょうか?

= 読み取りと書き込みに 1 桁のミリ秒の待機時間

### SQL Server のインデックスとは何かについて最もよく表している文はどれですか

= 検索が高速な方法で行を事前に並べ替えることで、行の取得を高速化します。インデックスが多すぎると、データベースの更新が遅くなり、効率が低下する可能性があるため、バランスを取る必要があります。

### コマンド ラインから Azure SQL Database をクエリできるツールには、次のどれがありますか

= sqlcmd

### Azure Blob Storage に Premium アクセス層を使用すると、何が得られますか?

= 高いトランザクションレートと1桁の一貫したストレージレイテンシに最適化されています。

### データ分析プラットフォームのコンテキスト内にエンドツーエンドの機械学習環境が含まれているのはどの Azure サービスですか

= Azure Databricks

### Azure Data Factory がサポートする 3 種類のアクティビティは何ですか?

= データ移動、データ変換、および制御

### グラフ データに最適な CosmosDB API 形式はどれですか

= Gremlin API

### どの Azure ストレージ ソリューションで、ストレージを Windows サーバーにドライブとしてマウントし、SMB プロトコルを使用してドライブ文字を割り当てることができるか

= Azure Files

### Azure ファイル共有の標準レベルと Premium レベルの主な違いは何ですか?

= Azure File Storage の標準層は、ハード ディスク ドライブ (HDD) の回転するマゼンティック ディスクに基づいていますが、Premium 層はソリッド ステート ドライブ (SSD) でより高速なパフォーマンスを提供します。

### 独自の環境内で SQL Server が実行されている。ご存じのとおり、あまり頻繁には使用されず、社内の管理アプリケーションのバックエンドとしてのみ使用され、SQL Server の標準機能のみが使用されています。あなたの目標はお金を節約することです。どの SQL データベースを Azure 内部に移行することをお勧めしますか

= 小規模な Azure SQL Database の単一インスタンス

### 大規模で複雑で、処理にかなりの時間がかかるデータの処理に最適なデータ分析ワークロードは、どのタイプですか

= バッチ処理

### リレーショナルデータベースのストアドプロシージャを最もよく表すステートメントはどれですか?

= 1 つ以上の Transact-SQL ステートメントのグループです。

### リレーショナル・データ表に主キーを持つことの結果は、次のうちどれですか

= テーブルには、テーブル内の各行を一意に識別する値を含む列または列の組み合わせがあります。

### 誰かがレポートをページ分割されたレポートとしてデザインする理由の1つは何ですか?

= 画面上に何千もの行を印刷する代わりに、それらをページ分割してナビゲートしやすくします。また、印刷することもできます。

### 次のステートメントのうち、Azure Databricks を最もよく表しているのはどれですか

= アナリストがデータに対してクエリを実行するための SQL プラットフォーム、データ サイエンティストとエンジニアが協力してデータ サイエンスの難しい問題に取り組むためのプラットフォーム

### さまざまなサイズの色付きの四角形はどのデータ視覚化スタイルですか?

= ツリーマップ

### データベースの構造の作成と変更に使用される SQL クエリ言語の種類はどれですか

= DDL はデータ定義言語であり、データ スキーマ要素の作成に使用されます。

### 次のメトリックのうち、Azure テーブル ストレージ アカウントのコストに影響するのはどれですか

= 配置されているリージョン、レプリケーション オプション、消費されたストレージ、および 100 万トランザクションあたり数セントに基づいて課金されます。

### どのようなタイプの非リレーショナル・データがノードとノード間の関係を中心に展開していますか。これは、会社の組織図やソーシャルネットワークなど、ノードから関連ノードに移動する必要があるデータベースに適したデータ型です

= グラフデータベースを使用すると、データ自体だけでなく、データ(エッジ)間の関係を簡単にナビゲートできます。

### リレーショナルデータベース内の「エンティティ」を表すデータ構造は何ですか?

= テーブルは、顧客、注文、製品など、現実世界で一意のエンティティを表します。

### 非リレーショナルデータベースをプロビジョニングするどの方法が、これを1回だけ行う必要があるユーザーにとって最適でしょうか?

= タスクを 1 回だけ実行する必要がある場合、またはカスタム構成が必要な場合は、Azure Portal を使用してリソースを作成するのが最適なアプローチです。

### アプリケーションが必要とする十分な 1 秒あたりの要求ユニット数 (RU/s) をプロビジョニングしない場合はどうなりますか

= 要求は、それを満たすために使用できるリソースがあることが望ましいときに、後で再試行するように要求されます。

### ETLはデータのプライバシーとコンプライアンスにどのように役立ちますか?

= 機密性の高い fiel や個人を特定できる情報を除外できることを意味します。
